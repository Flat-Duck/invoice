<div class="page"><header class="page-header"><div><h1>{{ __('Administrations') }}</h1><p>{{ __('Manage the administrations available when recording invoices.') }}</p></div><button class="btn primary" wire:click="create">{{ __('+ Add administration') }}</button></header>
@if(session('message'))<div class="toast">{{ session('message') }}</div>@endif @error('delete')<div class="alert">{{ $message }}</div>@enderror
<section class="panel"><div class="toolbar"><div class="search"><span>⌕</span><input wire:model.live.debounce.300ms="search" placeholder="{{ __('Search administrations') }}"></div><button class="btn" wire:click="$refresh">{{ __('Refresh') }}</button></div><div class="table-wrap"><table><thead><tr><th>{{ __('Administration') }}</th><th>{{ __('Notes') }}</th><th>{{ __('Invoices') }}</th><th></th></tr></thead><tbody>
@forelse($administrations as $administration)<tr><td><strong>{{ $administration->name }}</strong></td><td>{{ $administration->notes ?: '—' }}</td><td>{{ $administration->invoices_count }}</td><td class="actions"><button wire:click="edit({{ $administration->id }})">{{ __('Edit') }}</button><button class="danger-link" wire:click="delete({{ $administration->id }})" wire:confirm="{{ __('Delete this administration?') }}">{{ __('Delete') }}</button></td></tr>@empty<tr><td colspan="4"><div class="empty">{{ __('No administrations match your search.') }}</div></td></tr>@endforelse
</tbody></table></div><div class="pagination">{{ $administrations->links() }}</div></section>
@if($showForm)<div class="modal-backdrop" wire:click.self="$set('showForm',false)"><form class="modal" wire:submit="save"><div class="modal-head"><div><h2>{{ $editingId ? __('Edit administration') : __('Add administration') }}</h2><p>{{ __('This name will appear in the invoice dropdown.') }}</p></div><button type="button" wire:click="$set('showForm',false)">×</button></div><div class="form-grid"><label class="full">{{ __('Administration name') }}<input wire:model="name" autofocus>@error('name')<small class="error">{{ $message }}</small>@enderror</label><label class="full">{{ __('Notes') }}<textarea wire:model="notes" rows="3"></textarea>@error('notes')<small class="error">{{ $message }}</small>@enderror</label></div><div class="modal-actions"><button type="button" class="btn" wire:click="$set('showForm',false)">{{ __('Cancel') }}</button><button class="btn primary">{{ __('Save administration') }}</button></div></form></div>@endif</div>
