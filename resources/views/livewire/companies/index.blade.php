<div class="page"><header class="page-header"><div><h1>{{ __('Companies') }}</h1><p>{{ __('Manage customers and their invoice history.') }}</p></div><button class="btn primary" wire:click="create">{{ __('+ Add company') }}</button></header>
@if(session('message'))<div class="toast">{{ session('message') }}</div>@endif @error('delete')<div class="alert">{{ $message }}</div>@enderror
<section class="panel"><div class="toolbar"><div class="search"><span>⌕</span><input wire:model.live.debounce.300ms="search" placeholder="{{ __('Search companies or contacts') }}"></div><button class="btn" wire:click="$refresh">{{ __('Refresh') }}</button></div><div class="table-wrap"><table><thead><tr><th>{{ __('Company') }}</th><th>{{ __('Business Field') }}</th><th>{{ __('Contact') }}</th><th>{{ __('Email') }}</th><th>{{ __('Phone') }}</th><th>{{ __('Invoices') }}</th><th></th></tr></thead><tbody>
@forelse($companies as $company)<tr><td><strong>{{ $company->name }}</strong><small class="cell-note">{{ Str::limit($company->address,45) }}</small></td><td>{{ $company->business_field ?: '—' }}</td><td>{{ $company->contact_person ?: '—' }}</td><td>{{ $company->email ?: '—' }}</td><td>{{ $company->phone ?: '—' }}</td><td>{{ $company->invoices_count }}</td><td class="actions"><button wire:click="edit({{ $company->id }})">{{ __('Edit') }}</button><button class="danger-link" wire:click="delete({{ $company->id }})" wire:confirm="{{ __('Delete this company?') }}">{{ __('Delete') }}</button></td></tr>@empty<tr><td colspan="7"><div class="empty">{{ __('No companies match your search.') }}</div></td></tr>@endforelse
</tbody></table></div><div class="pagination">{{ $companies->links() }}</div></section>
@if($showForm)<div class="modal-backdrop" wire:click.self="$set('showForm',false)"><form class="modal" wire:submit="save"><div class="modal-head"><div><h2>{{ $editingId ? __('Edit company') : __('Add company') }}</h2><p>{{ __('Store the company and contact details.') }}</p></div><button type="button" wire:click="$set('showForm',false)">×</button></div><div class="form-grid"><label class="full">{{ __('Company name') }}<input wire:model="name" autofocus>@error('name')<small class="error">{{ $message }}</small>@enderror</label><label class="full">{{ __('Business Field') }}<input wire:model="business_field" placeholder="{{ __('Company business activity') }}">@error('business_field')<small class="error">{{ $message }}</small>@enderror</label><label>{{ __('Contact person') }}<input wire:model="contact_person"></label><label>{{ __('Email') }}<input type="email" wire:model="email">@error('email')<small class="error">{{ $message }}</small>@enderror</label><label>{{ __('Phone') }}<input wire:model="phone"></label><label class="full">{{ __('Address') }}<textarea wire:model="address" rows="2"></textarea></label><label class="full">{{ __('Notes') }}<textarea wire:model="notes" rows="3"></textarea></label></div><div class="modal-actions"><button type="button" class="btn" wire:click="$set('showForm',false)">{{ __('Cancel') }}</button><button class="btn primary">{{ __('Save company') }}</button></div></form></div>@endif</div>
