<div class="page"><header class="page-header"><div><h1>{{ __('Dashboard') }}</h1><p>{{ __('Your invoice business at a glance.') }}</p></div><a class="btn primary" href="{{ route('invoices') }}" wire:navigate>{{ __('+ New invoice') }}</a></header>
<section class="stats-grid">@foreach([[__('Companies'),$companyCount,__('Active records')],[__('Invoices'),$invoiceCount,__('All time')],[__('Total amount'),'$'.number_format($total,2),__('All invoices')],[__('This month'),'$'.number_format($monthTotal,2),__(now()->format('F')).' '.now()->year],[__('This year'),'$'.number_format($yearTotal,2),now()->year]] as [$label,$value,$note])<article><span>{{ $label }}</span><strong>{{ $value }}</strong><small>{{ $note }}</small></article>@endforeach</section>
<section class="panel"><div class="panel-head"><div><h2>{{ __('Recent invoices') }}</h2><p>{{ __('The latest activity in your local database.') }}</p></div><a href="{{ route('reports') }}" wire:navigate>{{ __('View reports →') }}</a></div><div class="table-wrap"><table><thead><tr><th>{{ __('Invoice') }}</th><th>{{ __('Company') }}</th><th>{{ __('Amount') }}</th><th>{{ __('Location') }}</th><th>{{ __('Date') }}</th></tr></thead><tbody>
@forelse($recent as $invoice)<tr><td class="link">{{ $invoice->invoice_number }}</td><td>{{ $invoice->company->name }}</td><td>${{ number_format($invoice->amount,2) }}</td><td>{{ $invoice->location }}</td><td>{{ $invoice->invoice_date->translatedFormat('M j, Y') }}</td></tr>@empty<tr><td colspan="5"><div class="empty">{{ __('No invoices yet. Add your first invoice to get started.') }}</div></td></tr>@endforelse
</tbody></table></div></section></div>
