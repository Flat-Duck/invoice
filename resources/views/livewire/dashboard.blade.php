<div class="page"><header class="page-header"><div><h1>Dashboard</h1><p>Your invoice business at a glance.</p></div><a class="btn primary" href="{{ route('invoices') }}" wire:navigate>+ New invoice</a></header>
<section class="stats-grid">@foreach([['Companies',$companyCount,'Active records'],['Invoices',$invoiceCount,'All time'],['Total amount','$'.number_format($total,2),'All invoices'],['This month','$'.number_format($monthTotal,2),now()->format('F Y')],['This year','$'.number_format($yearTotal,2),now()->year]] as [$label,$value,$note])<article><span>{{ $label }}</span><strong>{{ $value }}</strong><small>{{ $note }}</small></article>@endforeach</section>
<section class="panel"><div class="panel-head"><div><h2>Recent invoices</h2><p>The latest activity in your local database.</p></div><a href="{{ route('reports') }}" wire:navigate>View reports →</a></div><div class="table-wrap"><table><thead><tr><th>Invoice</th><th>Company</th><th>Amount</th><th>Location</th><th>Date</th></tr></thead><tbody>
@forelse($recent as $invoice)<tr><td class="link">{{ $invoice->invoice_number }}</td><td>{{ $invoice->company->name }}</td><td>${{ number_format($invoice->amount,2) }}</td><td>{{ $invoice->location }}</td><td>{{ $invoice->invoice_date->format('M j, Y') }}</td></tr>@empty<tr><td colspan="5"><div class="empty">No invoices yet. Add your first invoice to get started.</div></td></tr>@endforelse
</tbody></table></div></section></div>
